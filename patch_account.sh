sed -i 's/const \[meta, setMeta\] = useState<ConnectionMeta | null>(null);/const [meta, setMeta] = useState<ConnectionMeta | null>(null);\n  const [authProfile, setAuthProfile] = useState<DerivAccountProfile | null>(derivAuthService.getProfile());\n\n  useEffect(() => {\n    const interval = setInterval(() => {\n      setAuthProfile(derivAuthService.getProfile());\n    }, 1000);\n    return () => clearInterval(interval);\n  }, []);/' src/views/AccountView.tsx
